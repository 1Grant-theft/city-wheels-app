services:
  - type: web
    name: city-wheels-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: MYSQL_URL
        sync: false
